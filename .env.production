# Production environment – GitHub Pages static deployment
# This app is fully client-side; no backend API calls are made from the
# frontend. All data is stored in IndexedDB (Dexie) in the user's browser.
NODE_ENV=production
